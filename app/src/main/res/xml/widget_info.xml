<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget metadata.

  Everything about the layout is proportional to the widget's own size, so it can
  be resized freely. The minimums are deliberately low: the two figures sit side
  by side rather than stacked, which lets the whole thing stay usable when shrunk
  right down.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/widget_loading"
    android:minWidth="180dp"
    android:minHeight="200dp"
    android:targetCellWidth="4"
    android:targetCellHeight="5"
    android:minResizeWidth="180dp"
    android:minResizeHeight="200dp"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="3600000"
    android:previewLayout="@layout/widget_loading"
    android:widgetCategory="home_screen" />
