## What this changes

<!-- A sentence or two. If it fixes an issue, link it: Fixes #123 -->

## Why

<!-- What problem does this solve? -->

## How it was verified

- [ ] `./gradlew spotlessCheck` passes
- [ ] `./gradlew lintDebug` passes
- [ ] `./gradlew testDebugUnitTest` passes
- [ ] `./gradlew assembleDebug` succeeds
- [ ] Tested on a device or emulator (say which)

<!-- For calculator changes, list the expressions you checked.
     For currency changes, list the pairs. -->

## Notes for the reviewer

<!-- Anything surprising, or a decision you would like a second opinion on. -->
